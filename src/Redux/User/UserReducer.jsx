import {
    REGISTER_SUCCESS

} from "../ActionTypes";

const initState = {
    user_data: []
};

const userReducer = (state = initState, action) => {

    switch (action.type) {
        default:
            return state;
    }
};

export default userReducer;